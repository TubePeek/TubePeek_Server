module.exports = {
  // must return userId
  signUserUp: function (firstName, lastName, emailAddress, birthDay, gender) {
    
  },
  // must return userId
  logUserIn: function (emailAddress) {

  }
};
